# v0.9.1 Fix #2: Default to Latest Year

**Target**: On map load, show only the latest (highest numbered) year by default

---

## Changes Needed

After `addLayersControl()` call (~line 2330), add logic to hide all non-latest years:

```r
# After addLayersControl()
if (length(all_years) > 0) {
  latest_year <- max(all_years)
  for (yr in all_years) {
    if (yr != latest_year) {
      map <- hideGroup(map, paste0("Year ", yr))
    }
  }
}
```

**Note**: Need to identify where `all_years` comes from in the existing code. Likely already exists or can be derived from the year groups.

---

## Test

Generate map with multiple years (e.g., 2022, 2023, 2024):
- [ ] Only 2024 visible on load
- [ ] Other years hidden but accessible via radio buttons
- [ ] Schools layer always visible

---

## Commit

```
feat(ui): Default to showing latest year on map load

- Use hideGroup() to hide all non-latest years
- No new parameters or API changes
- Users can still switch years via radio buttons

Addresses: Issue #14
```
