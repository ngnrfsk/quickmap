# v0.9.1 Fix #1: Radio Buttons Bottom-Right + Collapsible

**Target**: Move year radio buttons to bottom-right corner, make them collapsible

---

## Changes Needed

Find `addLayersControl()` in `create_pollution_map()` (~line 2320-2330):

**Change this**:
```r
position = "topright",
options = layersControlOptions(collapsed = FALSE)
```

**To this**:
```r
position = "bottomright",
options = layersControlOptions(collapsed = TRUE)
```

That's it.

---

## Test

Generate a map with multiple years, open HTML, check:
- [ ] Radio buttons in bottom-right corner (opposite zoom)
- [ ] Starts collapsed
- [ ] Expands on click
- [ ] No overlap with zoom controls

---

## Commit

```
feat(ui): Move year control to bottom-right and make collapsible

- Change position to "bottomright" (opposite zoom controls)
- Set collapsed = TRUE

Addresses: Issue #12
```
