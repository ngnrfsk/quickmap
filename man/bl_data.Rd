% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/breathe_london.R
\name{bl_data}
\alias{bl_data}
\title{Fetch Breathe London measurements}
\usage{
bl_data(
  borough = NULL,
  site = NULL,
  start,
  end,
  species = c("NO2", "PM25"),
  key = Sys.getenv("BREATHE_LONDON_KEY")
)
}
\arguments{
\item{borough}{London borough name, e.g. "Merton"}

\item{site}{Site code(s); fetched one at a time when several}

\item{start, end}{Period bounds, anything \code{as.POSIXct()} accepts}

\item{species}{Pollutants to request, from "NO2" and "PM25"}

\item{key}{API key; defaults to the BREATHE_LONDON_KEY environment
variable}
}
\value{
Hourly data.frame, one row per site-hour
}
\description{
Wraps /SensorData: hourly NO2 and PM2.5 for a borough or a set of sites
between two times, returned in quickmap's contract format (\code{siteCode},
\code{date}, \code{no2}, \code{pm25}, \code{lat}, \code{lon}). Periods longer than the API's
366-day window are fetched in chunks; transient errors (429, 5xx) are
retried with backoff.
}
\seealso{
Other breathe london: 
\code{\link{bl_sensors}()},
\code{\link{from_breathelondon}()}
}
\concept{breathe london}
