% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wind.R
\name{from_worldmet}
\alias{from_worldmet}
\title{Create a wind layer from worldmet data}
\usage{
from_worldmet(data = NULL, station = NULL, year = NULL)
}
\arguments{
\item{data}{Data frame with POSIXct \code{date}, wind speed \code{ws} (m/s) and
wind direction \code{wd} (degrees, meteorological). NULL to fetch instead.}

\item{station}{NOAA station code (e.g. \code{"037720-99999"} for Heathrow),
used when \code{data} is NULL.}

\item{year}{Year(s) to fetch when \code{data} is NULL.}
}
\value{
A \code{qm_wind} object.
}
\description{
Builds the wind input for \code{\link[=quickmap]{quickmap()}}'s \code{wind} argument from NOAA
Integrated Surface Database observations. Supply either a data frame
already fetched (e.g. via \code{\link[worldmet:importNOAA]{worldmet::importNOAA()}}, or anything with
\code{date}, \code{ws}, \code{wd} columns) or a station code plus year(s) to fetch.
Wind speed/direction are converted to U/V components and averaged to the
map's displayed time steps at render time; a uniform city-scale field is
assumed (one representative station).
}
\concept{wind}
