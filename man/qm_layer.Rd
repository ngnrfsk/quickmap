% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qm_layer.R
\name{qm_layer}
\alias{qm_layer}
\title{Create a qm_layer — the QuickMap atomic data unit}
\usage{
qm_layer(
  data,
  value_col = NULL,
  time_col = NULL,
  label_col = NULL,
  shape = NULL,
  name = "layer",
  shorten_labels = FALSE
)
}
\arguments{
\item{data}{An sf object, or a data.frame with \code{lat}/\code{lon} columns}

\item{value_col}{Name of the column holding the plotted value. If NULL,
inferred: a known pollutant column (no2, pm25, ...), else the single
numeric non-contract column.}

\item{time_col}{Name of the time column. If NULL, inferred: a Date/POSIXct
column, else a column named date/time/datetime/year_str that parses under
the grammar. Pass explicitly for non-date orderings (e.g. diurnal hours).}

\item{label_col}{Name of the column supplying marker labels (NULL = none;
inferred from \code{Label} or \code{School} columns when present)}

\item{shape}{Marker symbol. Friendly names "circle", "square", "diamond",
"triangle", "cross", "star" and "plus" are accepted, as is any exact
renderer shape name (e.g. "stadium", "plus-circle" — invalid names
error with the full list). NULL (default) lets the map assign one
automatically (solid symbols for time-varying layers, outline symbols
for static layers, cycling in layer order). A map-level \code{data_symbols}
argument to \code{\link[=quickmap]{quickmap()}} overrides this.}

\item{name}{Human-visible layer name (legends, controls, errors)}

\item{shorten_labels}{Drop the school type from the label column, so
"Abbotsbury Primary School" reads "Abbotsbury" and the type appears once
in the banner key instead of on every marker. Default FALSE, because it
changes what an existing map says. Names that would collide keep their
full form — see \code{\link[=shorten_school_names]{shorten_school_names()}}. Schools only for 1.0; the
layer is left alone when it has no label column.}
}
\value{
The data as a \code{qm_layer} (still an sf data.frame)
}
\description{
A \code{qm_layer} is an sf object in long format (one row per marker per time
step) carrying its layer metadata as attributes, so nothing is re-inferred
downstream. Most users never call this directly: \code{\link[=from_csv]{from_csv()}},
\code{\link[=from_rdata]{from_rdata()}} and \code{\link[=from_openair]{from_openair()}} build layers from files, and (from
roadmap item 4) \code{quickmap()} accepts file paths directly. Call \code{qm_layer()}
to map data you assembled yourself.
}
\details{
Canonical columns: \code{code} (stable marker key), \code{time_label} (display text
per time step, absent for static layers), \code{time_sort} (POSIXct sort key),
the value column named by \code{value_col}, \code{lat}, \code{lon}, \code{geometry}. Legacy
names (\code{siteCode}, \code{year_str}, \code{Latitude}/\code{Longitude}) are accepted and
normalised.
}
\seealso{
Other atomic unit: 
\code{\link{from_csv}()},
\code{\link{from_openair}()},
\code{\link{from_rdata}()},
\code{\link{qm_meta}()},
\code{\link{shorten_school_names}()}
}
\concept{atomic unit}
