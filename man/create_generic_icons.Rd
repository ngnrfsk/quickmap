% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quickmap.R
\name{create_generic_icons}
\alias{create_generic_icons}
\title{Build the marker symbols for one layer at one time step}
\usage{
create_generic_icons(
  data,
  icon_shape,
  pollutant = NULL,
  colour_scale = NULL,
  image_scale_factor = 1,
  layer_id = NULL
)
}
\arguments{
\item{data}{Layer data for this time step}

\item{icon_shape}{Validated shape name}

\item{pollutant}{Pollutant column name, or NULL for a static layer}

\item{colour_scale}{Name of the colour scale for pollutant layers}

\item{image_scale_factor}{Symbol scaling for static export; 1.0 for HTML.
Base sizes assume a 1200x1200 image, so other sizes use
\code{sqrt((width * height) / (1200 * 1200))}}

\item{layer_id}{Layer identifier (used by callers for grouping)}
}
\value{
leaflegend symbol list ready for \code{addMarkers()}
}
\description{
Every layer type goes through this one function; what differs is only the
shape and how the colour is chosen. A layer with a \code{Level} column and no
pollutant (schools) is coloured categorically from the schools scale; a
pollutant layer is coloured by threshold band; anything else falls back to
grey.
}
\details{
Outline shapes (plus, cross, star) are coloured through \code{color}, the stroke,
because they have no interior to fill; filled shapes are coloured through
\code{fillColor} and given a white stroke so that overlapping markers stay
separable.
}
\keyword{internal}
