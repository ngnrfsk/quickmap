% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qm_layer.R
\name{shorten_school_names}
\alias{shorten_school_names}
\title{Drop the school type from school names}
\usage{
shorten_school_names(name, level = NULL)
}
\arguments{
\item{name}{Character vector of school names.}

\item{level}{Category per school (the \code{Level} column), or NULL. Used only
to decide whether a shared short name is safe.}
}
\value{
Character vector the same length as \code{name}.
}
\description{
"Abbotsbury Primary School" becomes "Abbotsbury". The type is what the
banner key says once (\code{\link[=build_banner_key]{build_banner_key()}}), so repeating it on every
marker costs width and tells the reader nothing new: on the Merton print
set it cut the longest label from 47 characters to 32.
}
\details{
\strong{Two schools must never merge into one label.} Where shortening would
give two rows the same name, they keep their full names — unless their
\code{Level} differs, in which case the cross colour already separates them and
the short form is safe. "Harris Primary Academy Merton" and "Harris
Academy Merton" are different schools, and both keep their full names.

Written for the LB Merton AQAP print set (2026-08-05) and brought into the
package on 2026-08-17. The vocabulary is schools-only; hospitals and GP
surgeries need their own, which is the post-1.0 place-type YAML.
}
\seealso{
Other atomic unit: 
\code{\link{from_csv}()},
\code{\link{from_openair}()},
\code{\link{from_rdata}()},
\code{\link{qm_layer}()},
\code{\link{qm_meta}()}
}
\concept{atomic unit}
\keyword{internal}
