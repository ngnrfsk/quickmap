% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quickmap.R
\name{label_font_size}
\alias{label_font_size}
\title{Marker-label font size, as CSS}
\usage{
label_font_size(label_scale)
}
\arguments{
\item{label_scale}{Multiplier on \link{MARKER_LABEL_REM}.}
}
\value{
A CSS length, e.g. \code{"0.75rem"}.
}
\description{
In \code{rem}, so it needs no export scaling of its own. A static export scales
the root font size (see \code{\link[=inject_banner_legend_controls]{inject_banner_legend_controls()}}), which is how
banner, legend and year label stay in proportion; expressing marker labels
the same way puts them in that system instead of beside it. At
\code{label_scale = 1} they land on \code{MARKER_LABEL_REM}, matching the smallest
text in the legend at any export size.
}
\details{
The unit is not optional, and its absence is silent: leaflet puts
\code{textsize} straight into a CSS \code{font-size}, where a bare number is invalid
and the browser drops it, leaving the label at whatever it inherits. The
code passed \code{as.character(12 * label_sizing)} for years, so every value was
inert — unnoticed because the multiplier was always 1.0 and the ignored
value and the fallback were both 12.
}
\keyword{internal}
