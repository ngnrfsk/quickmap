% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quickmap.R
\name{label_font_size}
\alias{label_font_size}
\title{Marker-label font size, as CSS}
\usage{
label_font_size(label_sizing)
}
\arguments{
\item{label_sizing}{Multiplier on the 12px base.}
}
\value{
A CSS length, e.g. \code{"52px"}.
}
\description{
The unit is not optional, and its absence is silent. \code{labelOptions()} puts
\code{textsize} straight into a CSS \code{font-size}, where a bare number is invalid
and the browser drops it, leaving the label at whatever it inherits. The
code here passed \code{as.character(12 * label_sizing)} for years, so every
value was inert — unnoticed because \code{label_sizing} was always 1.0 and the
ignored value and the fallback were both 12.
}
\keyword{internal}
