% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quickmap.R
\name{trim_colour_scale}
\alias{trim_colour_scale}
\title{Generate HTML legend structure from colour_scale}
\usage{
trim_colour_scale(legend_scale, data_max = NULL)
}
\arguments{
\item{legend_scale}{Scale list from \code{\link[=load_colour_scale]{load_colour_scale()}}}

\item{data_max}{Largest value in the data, or NULL for no trimming}

\item{scale_name}{Name of scale in colour_scales list (e.g., "who_no2")}

\item{collapsed_mobile}{Should legend start collapsed on mobile (default TRUE)}
}
\value{
Character string containing complete HTML legend structure

The scale with \code{colours} and \code{labels} cut to the bands in use
}
\description{
Creates a collapsible legend with header, toggle arrow, and color-coded items.
Mobile responsive: automatically collapses on screens < 480px if enabled.
Optionally trims legend to show only ranges up to maximum data value.
}
\details{
Validates:
\itemize{
\item Scale exists in colour_scales
\item colours and labels arrays have matching lengths
Converts all colors to hex format for CSS compatibility
If data_max provided, filters legend to show only relevant ranges (minimum 2 items)
}
}
\seealso{
Other legend: 
\code{\link{calculate_max_range_width}()},
\code{\link{generate_indicator_bar}()},
\code{\link{generate_indicator_html}()},
\code{\link{get_symbol_for_index}()},
\code{\link{parse_legend_label}()},
\code{\link{ramp_position}()}
}
\concept{legend}
\concept{legend
Trim a colour scale to the bands the data actually reaches

A legend running to 100 µg/m³ under data topping out at 33 wastes most of
its width on bands nothing falls in. Extracted from
\code{\link[=generate_legend_html]{generate_legend_html()}} so the legend and the indicator bar drawn above it
are trimmed identically — if they disagreed, the bar would point at the
wrong block.}
\keyword{internal}
