% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quickmap.R
\name{import_csv_data}
\alias{import_csv_data}
\title{Read a diffusion-tube or schools CSV}
\usage{
import_csv_data(file_path, required_cols = c("Easting", "Northing"))
}
\arguments{
\item{file_path}{Path to the CSV, absolute or relative to DATA_PATH}

\item{required_cols}{Columns that must be present and non-missing}
}
\value{
List of \code{data} (the data.frame) and \code{value_columns} (everything that
is not a required column — the year columns for tube data)
}
\description{
Relative paths are resolved against DATA_PATH. Leading "X" characters that
\code{read.csv()} adds to numeric column names ("X2017") are stripped, so year
columns keep the names the user gave them. Rows missing any required column
(or a \code{Label}, where one is present) are dropped.
}
\keyword{internal}
