% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/breathe_london.R
\name{from_breathelondon}
\alias{from_breathelondon}
\title{Breathe London data as a quickmap layer}
\usage{
from_breathelondon(
  borough = NULL,
  site = NULL,
  start,
  end,
  pollutant = c("no2", "pm25"),
  period = "year",
  data_capture = 0.75,
  name = "breathe_london",
  key = Sys.getenv("BREATHE_LONDON_KEY")
)
}
\arguments{
\item{borough}{London borough name, e.g. "Merton"}

\item{site}{Site code(s); fetched one at a time when several}

\item{start, end}{Period bounds, anything \code{as.POSIXct()} accepts}

\item{pollutant}{Which pollutant the layer carries, "no2" or "pm25"}

\item{period}{Aggregation period, "year" or "month"}

\item{data_capture}{Minimum fraction of the period's hours that must
carry a value, default 0.75}

\item{name}{Layer name}

\item{key}{API key; defaults to the BREATHE_LONDON_KEY environment
variable}
}
\value{
A \code{\link[=qm_layer]{qm_layer()}}, diamond-shaped like the other sensor layers
}
\description{
Fetches, screens and aggregates in one call: hourly data via
\code{\link[=bl_data]{bl_data()}}, the plausibility screen (\code{\link[=bl_qa_screen]{bl_qa_screen()}}), then period
means that exist only where at least \code{data_capture} of the period's
hours were measured (\code{\link[=bl_period_means]{bl_period_means()}}) — an annual mean built from a
few weeks of data misleads, so it becomes NA instead.
}
\seealso{
Other breathe london: 
\code{\link{bl_data}()},
\code{\link{bl_sensors}()}
}
\concept{breathe london}
