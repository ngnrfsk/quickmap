% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quickmap.R
\name{generate_map_layers}
\alias{generate_map_layers}
\title{Draw every enabled layer onto a map for one time step}
\usage{
generate_map_layers(
  base_map,
  measurement_layers,
  target_year,
  pollutant,
  colour_scale,
  spatial_data,
  image_scale_factor = 1,
  label_scale = 1
)
}
\arguments{
\item{base_map}{Map to add to}

\item{measurement_layers}{Layer configuration from \code{\link[=get_measurement_layers]{get_measurement_layers()}}}

\item{target_year}{Time step, or "static_only" for static layers only}

\item{pollutant}{Pollutant column name}

\item{colour_scale}{Name of the colour scale}

\item{spatial_data}{Loaded layers from \code{\link[=load_spatial_data_sources]{load_spatial_data_sources()}}}

\item{image_scale_factor}{Symbol scaling for static export}

\item{label_scale}{Extra multiplier on marker-label text, over and above
\code{image_scale_factor}. Marker labels used to sit at a flat 12px however
large the export was (roadmap item 11, "unified marker/text/legend
scaling"), so a 4000px print came out with symbols scaled and their
labels not. They now follow the export; \code{label_scale} is the further
push a given page size needs — see the \code{map.label_scale} theme key.}
}
\value{
The map with this step's layers added
}
\description{
Temporal layers are filtered to \code{target_year} and added as a Leaflet group
named after that step, which is how the time control shows one step at a
time. Static layers carry no group and are drawn once; passing
\code{target_year = "static_only"} adds those alone.
}
\keyword{internal}
