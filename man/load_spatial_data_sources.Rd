% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quickmap.R
\name{load_spatial_data_sources}
\alias{load_spatial_data_sources}
\title{Load every data source and return them as spatial layers}
\usage{
load_spatial_data_sources(
  data_sources,
  data_ids = NULL,
  data_dynamic = NULL,
  pollutant = "no2"
)
}
\arguments{
\item{data_sources}{List of file paths and/or sf objects}

\item{data_ids}{Layer names; NULL derives them from the file names}

\item{data_dynamic}{Logical vector overriding the time-varying guess}

\item{pollutant}{Pollutant name, for RData loading}
}
\value{
List with \code{all_data} (named list of sf layers), \code{ids}, and the
legacy positional slots
}
\description{
Accepts sf objects (used as they are), RData files, and CSV files. A CSV is
treated as time-varying only when it has more than one time-shaped column
name: a single such column is far more likely to be a one-off measurement
set than an animation, and the user can override the guess with
\code{data_dynamic}.
}
\details{
The returned list carries both the general \code{all_data}/\code{ids} pair used by the
current pipeline and the older positional \code{dt}/\code{sensor}/\code{school} slots (first,
second and third source), which pre-v0.9.2 callers still read.
}
\keyword{internal}
