% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quickmap_api.R
\name{create_pollution_map}
\alias{create_pollution_map}
\title{Create interactive and/or static pollution maps}
\usage{
create_pollution_map(
  data_sources = NULL,
  data_ids = NULL,
  data_symbols = NULL,
  data_dynamic = NULL,
  output_file,
  output_dir = getOption("quickmap.output_dir", NULL),
  export_image = NULL,
  boroughs = NULL,
  pollutant = "no2",
  display_times = NULL,
  title = NULL,
  vignette = NULL,
  colour_scale = "who_no2",
  styling_type = "html",
  symbol_labels = NULL,
  banner_colour = NULL,
  boundary_labels = NULL,
  autoplay = NULL,
  play_speed = NULL,
  theme_file = NULL,
  wind = NULL,
  ...
)
}
\arguments{
\item{data_sources}{List of data inputs: CSV/RData file paths or sf objects}

\item{data_ids}{Optional layer IDs (NULL auto-generates from filenames)}

\item{data_symbols}{Optional marker symbols per layer}

\item{data_dynamic}{Optional logical vector forcing temporal (TRUE) or
static (FALSE) handling per layer (NULL = auto-detect)}

\item{output_file}{Required. Where to write the map: a bare name goes in
the working directory, a path goes where it says, creating the directory.}

\item{output_dir}{Directory prepended to \code{output_file}, and to any JPGs.
Default: the \code{quickmap.output_dir} option, else NULL.}

\item{export_image}{NULL, TRUE, or c(width, height) for JPG export}

\item{boroughs}{Character vector of borough names (or "All"). NULL
(default) draws no boundary and fits the map to the data.}

\item{pollutant}{Pollutant to display (e.g. "no2", "pm25")}

\item{display_times}{Time periods to display (NULL = all available)}

\item{title}{Banner title}

\item{vignette}{Dim the area outside the boundary}

\item{colour_scale}{Name of a bundled colour scale}

\item{styling_type}{"html" or "none"}

\item{symbol_labels}{FALSE, TRUE (hover), "values_on", "labels", "labels_on"}

\item{banner_colour}{Banner background colour}

\item{boundary_labels}{Show boundary name labels}

\item{autoplay}{Auto-advance the time control}

\item{play_speed}{Autoplay interval in ms. NULL takes the theme's value,
and failing that a pace set by the number of time steps}

\item{theme_file}{Path to a YAML theme file}

\item{wind}{Optional wind layer (see \code{\link[=quickmap]{quickmap()}})}

\item{...}{Accepts the old argument name \code{marker_labels}, with a warning.
Any other name is an error.}
}
\value{
The saved file is the product; the Leaflet object comes back
invisibly as a byproduct. Writes \code{output_file}, and JPGs when
\code{export_image} is set.
}
\description{
Compatibility wrapper around \code{\link[=quickmap]{quickmap()}} — the historic QuickMap entry
point, retained with its existing signature. It converts \code{data_sources}
(file paths or sf objects) into \code{\link[=qm_layer]{qm_layer()}} atomic units and delegates to
\code{\link[=quickmap]{quickmap()}}. New code should call \code{\link[=quickmap]{quickmap()}} directly.
}
\seealso{
Other map: 
\code{\link{add_boundary_polygons}()},
\code{\link{quickmap}()},
\code{\link{render_pollution_map}()}
}
\concept{map}
