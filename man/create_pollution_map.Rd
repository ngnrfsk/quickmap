% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quickmap.R
\name{create_pollution_map}
\alias{create_pollution_map}
\title{Create interactive and/or static pollution maps}
\usage{
create_pollution_map(
  data_sources = NULL,
  data_ids = NULL,
  data_symbols = NULL,
  data_dynamic = NULL,
  output_file = "pollution_map.html",
  export_image = NULL,
  boroughs,
  pollutant = "no2",
  display_times = NULL,
  title = NULL,
  vignette = NULL,
  colour_scale = "who_no2",
  styling_type = "html",
  marker_labels = NULL,
  banner_colour = NULL,
  boundary_labels = NULL,
  autoplay = NULL,
  play_speed = NULL,
  theme_file = NULL
)
}
\arguments{
\item{data_sources}{List of file paths or sf objects. Files can be CSV (diffusion tubes, schools)
or RData (sensor data). Prepends DATA_PATH if set.}

\item{data_ids}{Character vector of layer IDs (default: NULL, auto-generated from filenames).}

\item{data_symbols}{Character vector of marker symbols (default: NULL, auto-cycles through defaults).
Valid: "circle", "diamond", "cross", "square", "triangle", "star", "plus".}

\item{data_dynamic}{Logical vector indicating temporal (TRUE) vs static (FALSE) layers (default: NULL, auto-detected).}

\item{output_file}{Output filename (without extension). Saved to 'aq_maps/' directory.}

\item{export_image}{NULL (no export), TRUE (export 1200x1200), or c(width, height) vector for custom dimensions.}

\item{boroughs}{Borough name(s) for boundary display and data filtering (required).}

\item{pollutant}{Pollutant type: "no2" or "pm25" (default: "no2").}

\item{display_times}{Time periods to display (e.g., "2023", "2023-01", "2023-01-15 10:00").
NULL uses all available periods from data. Must match year_str format in data.}

\item{title}{Page title and banner text. Default: NULL (uses theme).}

\item{vignette}{If TRUE, darkens areas outside borough(s). Default: NULL (uses theme).}

\item{colour_scale}{Color scale name (default: "who_no2"). See \code{load_colour_scale()} for options.}

\item{styling_type}{Controls banner/legend display: "html" (default) or "none".}

\item{marker_labels}{Label visibility: FALSE, TRUE (hover), "values_on" (always), "labels" (custom/hover),
"labels_on" (custom/always). Default: NULL (uses theme). Content: School/Label/pollutant columns.}

\item{banner_colour}{Color for banner and vignette. Default: NULL (uses theme).}

\item{boundary_labels}{If TRUE, shows borough boundary labels. Default: NULL (uses theme).}

\item{autoplay}{Auto-start year animation on load. Default: NULL (uses theme).}

\item{play_speed}{Milliseconds per year during animation. Default: NULL (uses theme).}

\item{theme_file}{Path to YAML theme file (default: NULL). See inst/themes/ for examples.}
}
\value{
Invisible Leaflet map object. Side effects: Saves HTML to \code{aq_maps/}.
If \code{export_image} is not NULL, also saves JPG files (one per year).
}
\description{
Main function to create Leaflet maps showing air pollution data with optional
schools overlay. Generates both interactive HTML maps and static JPG exports.
}
\details{
\strong{Data Files:} CSV files (diffusion tubes, schools) require Easting/Northing columns.
RData files (sensors) require 'dataOAformat' object. School data auto-detected by School column.

\strong{Coordinates:} Input British National Grid (EPSG:27700), output WGS84 (EPSG:4326).

\strong{Setup:} Set \code{Sys.setenv(DATA_PATH = "~/path/to/data")} before use.

\strong{Symbols:} Auto-assigned by data type. Override with data_symbols parameter.
}
\note{
Year columns in CSV files must be YYYY format (e.g., "2024").
Static JPG exports require Chrome/Chromium for webshot2.
}
\examples{
# Basic interactive map (annual)
Sys.setenv(DATA_PATH = "~/data")
create_pollution_map(
  data_sources = list("wandsworth_2017_2024.csv", "schools_Wandsworth.csv"),
  boroughs = "Wandsworth",
  display_times = "2024",
  output_file = "wandsworth_2024.html"
)

# Monthly data (sub-annual)
create_pollution_map(
  data_sources = list("sensor_monthly.Rdata"),
  boroughs = "Merton",
  display_times = c("2024-01", "2024-02", "2024-03"),
  output_file = "merton_q1_2024"
)

}
\seealso{
Other map: 
\code{\link{add_boundary_polygons}()}
}
\concept{map}
