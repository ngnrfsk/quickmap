% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quickmap.R
\name{build_banner_key}
\alias{build_banner_key}
\title{Build the banner's reference-layer key}
\usage{
build_banner_key(measurement_layers, spatial_data)
}
\arguments{
\item{measurement_layers}{Layer configuration from \code{\link[=get_measurement_layers]{get_measurement_layers()}}}

\item{spatial_data}{Loaded layers from \code{\link[=load_spatial_data_sources]{load_spatial_data_sources()}}}
}
\value{
List with \code{shape} and \code{items} (label and colour), or NULL when no
layer qualifies.
}
\description{
A static reference layer carries no value, so it gets no place on the
colour ramp and its symbol would otherwise go unexplained. Where such a
layer has a \code{Level} column, its categories and their colours become a small
key at the end of the banner — "✕ Primary, ✕ Secondary" — which is also
what lets the map labels drop the words "Primary School" and stay legible.
}
\keyword{internal}
