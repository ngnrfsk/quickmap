% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quickmap.R
\name{determine_times_and_viewport}
\alias{determine_times_and_viewport}
\title{Decide which time steps to show and where the map should look}
\usage{
determine_times_and_viewport(
  spatial_data,
  borough_sf,
  vignette,
  requested_times
)
}
\arguments{
\item{spatial_data}{Loaded layers from \code{\link[=load_spatial_data_sources]{load_spatial_data_sources()}}}

\item{borough_sf}{Boundary polygons, or NULL}

\item{vignette}{Logical; whether the dimming shape is wanted}

\item{requested_times}{The user's \code{display_times}, or NULL for all}
}
\value{
List of \code{primary_data}, \code{display_times}, \code{vignette_overlay} and
\code{bbox}
}
\description{
The time steps come from the first time-varying layer: with several such
layers one has to set the animation's steps, and the first is the one the
user listed first. A map with no time-varying layer at all is marked
"static_only", which suppresses the time control downstream.
}
\details{
The viewport follows the boundary when there is one, and otherwise the
combined extent of the data (boroughs became optional in v0.9.9.6).
}
\keyword{internal}
