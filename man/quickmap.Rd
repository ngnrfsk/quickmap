% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/quickmap_api.R
\name{quickmap}
\alias{quickmap}
\title{Build an interactive air quality map}
\usage{
quickmap(
  layers,
  boroughs,
  pollutant = NULL,
  display_times = NULL,
  colour_scale = "who_no2",
  output_file = "quickmap.html",
  title = NULL,
  styling_type = "html",
  export_image = NULL,
  marker_labels = NULL,
  vignette = NULL,
  banner_colour = NULL,
  boundary_labels = NULL,
  autoplay = NULL,
  play_speed = NULL,
  theme_file = NULL,
  data_symbols = NULL
)
}
\arguments{
\item{layers}{One layer or a list of layers. Each element may be a file
path (CSV or RData, resolved against \code{DATA_PATH}), a \code{\link[=qm_layer]{qm_layer()}}, or a
data.frame/sf object (converted via \code{\link[=qm_layer]{qm_layer()}}).}

\item{boroughs}{Character vector of borough names (or "All")}

\item{pollutant}{Pollutant to display. NULL (default) infers it from the
first time-varying layer's value column.}

\item{display_times}{Time periods to display (NULL = all available)}

\item{colour_scale}{Name of a bundled colour scale (see inst/config/scales/)}

\item{output_file}{HTML file name written into \verb{aq_maps/}}

\item{title}{Banner title}

\item{styling_type}{"html" (banner + legend + time control) or "none"}

\item{export_image}{NULL, TRUE, or c(width, height) for JPG export}

\item{marker_labels}{FALSE, TRUE (hover), "values_on", "labels", "labels_on"}

\item{vignette}{Dim the area outside the boundary}

\item{banner_colour}{Banner background colour}

\item{boundary_labels}{Show boundary name labels}

\item{autoplay}{Auto-advance the time control}

\item{play_speed}{Autoplay interval in ms}

\item{theme_file}{Path to a YAML theme file}

\item{data_symbols}{Optional character vector of marker symbols per layer
(default: automatic assignment — solid symbols for time-varying layers,
outline symbols for static layers)}
}
\value{
Invisible Leaflet map object; writes HTML (and optionally JPGs) to
\verb{aq_maps/}
}
\description{
The core QuickMap entry point. Takes one layer or a list of layers — file
paths, \code{\link[=qm_layer]{qm_layer()}} objects, or data.frames — and renders a self-contained
interactive HTML map (optionally with static JPG export). A two-line call
works; each additional parameter unlocks more sophistication.
}
\section{Design rule — where parameters live}{

Properties of a \emph{layer} (its value column, time column, label column,
symbol shape, name) belong on the layer itself, set by \code{\link[=qm_layer]{qm_layer()}} or the
\verb{from_*()} wrappers. Properties of the \emph{map} (boroughs, colour scale,
title, theme, output) belong here. \code{quickmap()} therefore has no per-layer
parallel-vector arguments: to customise one layer of several, customise
that layer — e.g.
\code{quickmap(list(from_csv("tubes.csv"), qm_layer(d, value_col = "pm25")), ...)}.
}

\examples{
\dontrun{
# two lines: a usable map
library(quickmap)
quickmap("wandsworth_2017_2024.csv", boroughs = "Wandsworth")

# several layers plus styling
quickmap(
  list("merton_dt_2018_2024.csv", "bl_sensors.Rdata", "schools_Merton.csv"),
  boroughs = "Merton",
  colour_scale = "who_no2",
  title = "Merton NO2",
  output_file = "merton_no2.html"
)

# expert: hand-built atomic unit
quickmap(qm_layer(my_data, value_col = "pm25"), boroughs = "Richmond")
}
}
\seealso{
Other map: 
\code{\link{add_boundary_polygons}()},
\code{\link{create_pollution_map}()},
\code{\link{render_pollution_map}()}
}
\concept{map}
